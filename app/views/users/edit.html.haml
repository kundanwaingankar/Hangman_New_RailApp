%h1 Update record
=form_for(@users) do |f|
  .errors
    -if @users.errors.any?
      %p An error occurred
    -@users.errors.full_messages.each do |msg|
      %li= msg
  .name
    =f.label :User_name
    =f.text_field :user_name
  .password
    =f.label :password
    =f.text_field :password
  =f.submit

